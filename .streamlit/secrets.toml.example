# Copy this file to .streamlit/secrets.toml and replace with your real password.
# .streamlit/secrets.toml is gitignored so it will not be committed.
#
# For Streamlit Community Cloud deployment, paste the same key/value into the
# "Secrets" section of your app settings instead of committing this file.

app_password = "change-me"
